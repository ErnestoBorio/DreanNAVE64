{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build & Launch Retro Debugger",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "Build PRG",
            "runtimeExecutable": "${workspaceFolder}/run_debugger.sh",
            "console": "integratedTerminal"
        }
    ]
}
